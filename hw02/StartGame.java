/*
package cs3500.hw02;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;
*/
/**
 * Created by Tigran on 5/15/2017.
 *//*

public class StartGame {
    List<getDeck> deck;
    private final  int numCascades = 8;
    private final  int numOpens = 4;
    private boolean shuffle;


    //constructor
    public StartGame() throws IllegalAccessException {

    if(isValidDeck() != true){
        throw new IllegalAccessException("The deck being used is not valid");
    }else{


    }



    }
    private static boolean isUnique(List deck){
        Set inputSet = new HashSet(deck);
        if(inputSet.size() == deck.size()){ // since Set does not contain duplicate, size must be less for an array which contains duplicate
            return true;
        }else{
            return false;
        }
    }

    private boolean isValidDeck() {
       if(deck.size() != 52){
           return false;
       }else {
           return isUnique(deck);
       }
    }
}



*/
